apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kanboard
  name: kanboard
  namespace: kanboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kanboard
  template:
    metadata:
      labels:
        app: kanboard
    spec:
      serviceAccountName: kanboard
      containers:
      - image: ghcr.io/thekoma/samesoup:latest
        imagePullPolicy: Always
        name: kanboard
        readinessProbe:
          httpGet:
            path: /ping.php
            port: 80
        livenessProbe:
          httpGet:
            path: /ping.php
            port: 80